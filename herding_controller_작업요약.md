# UnderGuard AMR — 몰이(Herding) 알고리즘 패키지 개발 작업 요약

**기간:** 2026-08-04 ~ 2026-08-05
**결과물:** `herding_controller` ROS2(Humble) 패키지 — 로봇 2대 협업 표적 몰이 알고리즘
**위치:** `~/Intelligence1_Algorithm/herding_controller/`
**최종 상태:** 전체 완료, 단위 테스트 133개 전부 통과, ALGO-001~008 중 7/8 PASS

---

## 1. 프로젝트 개요

- 로봇 2대가 협업해 표적(쥐 역할의 미니카)을 실시간 추적하고 지정된 포획 구역으로 몰아넣는 알고리즘.
- 핵심 아이디어(Shepherding): 표적은 로봇이 다가오면 반대 방향으로 도망친다 → 로봇은 표적을 목표 지점 반대편에서 밀어야 한다.
- 로봇 2대는 **Driver**(표적 뒤에서 미는 역할)와 **Blocker**(도주 경로를 선점 차단하는 역할)를 상황에 따라 동적으로 교대.
- 기존 팀 프로젝트(perception, mission manager, Nav2)와 결합할 것을 전제로, **ROS 의존성 없는 순수 Python 코어**로 설계 → 오프라인 시뮬레이션 검증이 가능하고, ROS 노드는 그 위에 얇은 어댑터로만 얹음.

## 2. 진행 방식

1. 사용자가 작성한 상세 작업 지시서(`prompt_herding_controller.md`)를 받아 설계 스펙으로 정리.
2. 15개 태스크로 나눈 TDD 기반 구현 플랜 작성 (각 태스크: 실패하는 테스트 작성 → 최소 구현 → 통과 확인 → 커밋).
3. **Subagent-Driven Development** 방식으로 진행: 태스크마다 별도의 "구현자" 서브에이전트를 띄워 구현시키고, 완료되면 별도의 "리뷰어" 서브에이전트가 스펙 준수 여부와 코드 품질을 독립적으로 검증. 문제가 발견되면 수정 라운드를 거쳐 재검토.
4. 15개 태스크가 모두 끝난 뒤, **전체 브랜치에 대한 최종 리뷰**를 별도로 한 번 더 수행 — 개별 태스크 리뷰로는 보이지 않는 "태스크 간 이음매" 버그를 찾기 위함.
5. 이 방식 덕분에, 제가 처음에 작성한 구현 플랜(레퍼런스 코드 포함)에 있던 버그들도 구현/리뷰 단계에서 거의 대부분 잡아낼 수 있었음.

## 3. 구현된 핵심 모듈 (15개 태스크)

| # | 모듈 | 내용 |
|---|---|---|
| 1 | 패키지 골격 | ROS2 ament_python 패키지 구조 + `herding_params.yaml` (임계값 전부 외부화) |
| 2 | `grid_map.py` | 격자 ↔ 실좌표 변환, 장애물 마스크 |
| 3 | `target_estimator.py` | 상수속도 모델 칼만 필터 — 표적 위치/속도 추정 |
| 4 | `escape_model.py` | 마르코프 모델 기반 도주 방향 예측 (벽 선호 + 로봇 반발 + 관성) |
| 5 | `herding_planner.py` | Driving Point(미는 지점) / Blocking Point(차단 지점) 계산 |
| 6 | `role_assigner.py` | 히스테리시스 기반 Driver/Blocker 동적 배정 (역할 진동 방지) |
| 7 | `state_machine.py` | 6상태 FSM: IDLE→SEARCH→TRACK→HERD→CORNER→CAPTURED (+LOST 복구) |
| 8 | `occlusion_grid.py` | 표적 시야 차단(LOST) 시 확산/감쇠 베이지안 재탐색 격자 |
| 9 | `herding_core.py` | 위 전부를 조합하는 파사드 — **ROS 의존성 0**, 여기가 핵심 아키텍처 제약 |
| 10 | 도주 모델 5종 | `reactive_flee`, `wall_hugger`, `noisy_human`(실물 예측용), `random_walk`(대조군), `log_replay` |
| 11 | `simulator.py` | ROS 없는 2D 물리 시뮬레이터 (검증용) |
| 12 | `run_validation.py` | ALGO-001~008 자동 통계 검증 + 카이제곱 대조군 실험 + 그래프 생성 |
| 13 | `herding_node.py` | rclpy 노드 — **패키지에서 유일하게 ROS를 import하는 파일** |
| 14 | 조종자 프로토콜 | `operator_protocol.md` + 실물 시연용 CSV 로거(`field_logger.py`, 편향 통제) |
| 15 | 최종 검증 + 튜닝 | 100회 실행 통계 검증, 파라미터 튜닝, 최종 리포트 작성 |

## 4. 최종 검증 결과 (N=100)

| 항목 | 기준 | 측정값 | 결과 |
|---|---|---|---|
| ALGO-001 성공률 | ≥70% | **83.0%** | ✅ PASS |
| ALGO-002 평균 포획 시간 | ≤60초 | **50.1초** | ✅ PASS |
| ALGO-003 패닉(과근접) 발생률 | ≤10% | **6.0%** | ✅ PASS |
| ALGO-004 역할 진동(교대 횟수) | ≤5회 | **최대 1회** | ✅ PASS |
| ALGO-005 제어 주기 지연 | ≤100ms | **0.3ms** | ✅ PASS |
| ALGO-006 시야 차단 후 재탐색 | ≥80% | **79.3%** | ❌ **FAIL** |
| ALGO-007 파라미터 외부화 | 하드코딩 0건 | 구조적 보장 | ✅ PASS |
| ALGO-008 대조군 대비 유의성 | ≥40%p, p<0.05 | **+76.0%p, p<0.0001** | ✅ PASS |

- **8개 중 7개 통과.**
- ALGO-006만 실패했는데, 파라미터를 아무리 튜닝해도 안 되는 **구조적 한계**임을 확인 (재탐색 격자의 확산 모델에 "이동 방향" 개념이 없어서, 표적이 어느 쪽으로 갔는지 반영을 못 함 — 고치려면 알고리즘 자체에 이동 모델 추가가 필요). 억지로 숫자를 맞추기보다 **정직하게 FAIL로 보고**.
- 실물 미니카 시연 시 예상 성공률(사람이 조종하는 것과 가장 유사한 `noisy_human` 모델 기준): **90.0%**

## 5. 개발 과정에서 발견하고 고친 주요 버그들

이 프로젝트의 특징은, 제가 처음에 작성한 구현 플랜의 "레퍼런스 코드" 자체에도 버그가 여럿 있었는데, 구현 단계 또는 리뷰 단계에서 거의 다 잡아냈다는 점입니다. 대표적인 것들:

- **패닉 방지 로직**: 로봇이 표적에 너무 가까이 붙으면 후퇴해야 하는데, 원래 코드는 "누적 최솟값"을 기준으로 판정해서 한 번 위반하면 이후 모든 사이클을 위반으로 계속 카운트하는 버그.
- **역할 배정 쿨다운 우회**: 로봇 역할(Driver/Blocker) 교대 시 진동 방지용 쿨다운이, 맨 처음 배정 시에는 우회되어버리는 버그.
- **시뮬레이터에 벽이 없음**: 검증용 가상 아레나에 실제 벽 충돌 처리가 없어서 표적이 격자 밖으로 이탈해버리는 버그 → 발견 후 벽을 추가했더니, 도주 모델이 애초에 장애물 정보를 못 받고 있던 **2차 버그**까지 연쇄로 발견.
- **막힌 상태에서 확률 0**: 표적이 장애물에 완전히 둘러싸이면 도주 확률 분포가 전부 0이 되어버려 다운스트림 로직이 깨지는 버그.
- **최종 전체 리뷰에서 발견한 것 (개별 태스크 리뷰로는 안 보이던 것)**:
  - 노드가 표적 위치를 "새로 안 들어옴"으로 인식하는 로직이 없어서, 시야 차단(LOST) 상태로 절대 못 넘어가는 버그 → **실전에서 LOST 복구 기능 전체가 무용지물**이 될 뻔함.
  - 15번 태스크에서 파라미터를 튜닝했는데, ROS 노드의 "기본값"은 튜닝 전 값 그대로 남아있어서, 런치 파일 없이 노드만 단독 실행하면 성공률이 83%→2.5%로 붕괴하는 설정이 기본으로 걸려있던 버그.

## 6. 확인이 필요한 항목 (사용자 결정 필요)

1. **`compute_driving_point`에 장애물 회피 로직 없음**: Driver 로봇의 목표 좌표 계산 시 장애물 체크를 안 해서, 발행되는 목표 좌표의 약 25%가 장애물(벽) 안쪽에 찍힘. 시뮬레이션 성능 자체에는 영향이 미미했지만(+1.6%p 정도), **실제 ROS 환경에서는 미션 매니저가 이 좌표를 검증 없이 Nav2에 바로 넘기면 안 됨** — 반드시 장애물 유효성 검사를 거치도록 통합 필요.
2. **`~/escape_probability` 토픽의 의미**: RViz 시각화용 토픽인데, 어떤 상태에서 무엇을 보여줄지 스펙 문서상 다소 모호했던 부분이라 실제 팀 통합 시 재확인 권장.
3. **ALGO-006(시야 차단 복구)을 실제로 고치려면**: `occlusion_grid.py`의 확산 모델에 표적의 마지막 속도 방향을 반영하는 "이동 모델" 추가가 필요함 (파라미터 튜닝으로는 불가능하다고 검증됨).
4. **도주 모델 파일 목록 관련 사소한 스펙 불일치**: 원본 지시서의 두 섹션(3-1, 3-2)에서 요구한 파일명이 서로 달랐음 — 더 상세한 쪽(3-2, 검증 기준표)을 따라 구현함.

## 7. 산출물 위치

- 코드: `herding_controller/herding_controller/` (핵심 로직), `herding_controller/test/` (시뮬레이터, 검증 스크립트, 테스트)
- 최종 검증 리포트: `docs/superpowers/plans/2026-08-04-herding-controller-final-report.md`
- 조종자 프로토콜(실물 시연용): `herding_controller/docs/operator_protocol.md`
- 설계 스펙: `docs/superpowers/specs/2026-08-04-herding-controller-design.md`
- 구현 플랜(15개 태스크 상세): `docs/superpowers/plans/2026-08-04-herding-controller.md`
- Git 커밋: 로컬 `master` 브랜치에 전부 기록 (원격 저장소 없음, 총 30여 개 커밋)

## 8. 통계로 보는 작업 규모

- 총 15개 구현 태스크 + 최종 전체 리뷰 1회
- 대부분의 태스크에서 구현자가 리뷰 전에 스스로 버그를 찾아 고침 (레퍼런스 코드 결함 포함)
- 여러 태스크에서 리뷰어가 추가로 1~2건씩 중요 버그를 더 발견 → 수정 라운드 진행
- 최종 전체 리뷰에서 Critical 2건 + Important 4건의 "태스크 간" 버그 발견 → 전부 수정 및 재검증 완료
- 단위 테스트: 최종 133개 전부 통과
