# UnderGuard — 프로젝트 컨텍스트

> 새 세션은 이 파일을 먼저 읽는다. 여기 없는 배경은 모른다고 가정하고 시작할 것.

## 프로젝트

실내 순찰·방역 로봇 시스템. TurtleBot 4 두 대가 방을 순찰하다 쥐(사람이 조종하는
RC카, 18×9cm)를 발견하면 **협공으로 지정된 덫 좌표까지 몰아넣는다.**

- ROS 2 Humble
- 주 브랜치: `algorithm/intelligence1-herding-blocker-redesign` (main보다 100커밋 이상 앞섬, 아직 미병합)

## 파트 분담 — 파일 소유권을 지킬 것

| 경로 | 담당 | 비고 |
|---|---|---|
| `intelligence1_algorithm/**` | **알고리즘 (사용자)** | 이 저장소에서 우리가 만지는 곳 |
| `src/turtle_project/turtle_project/rat_herding_node.py` | **알고리즘 (사용자)** | 알고리즘↔fleet 배관, 경계 파일 |
| `src/**` 나머지 전부 | 로봇 파트 팀원 | **건드리지 말 것.** 필요하면 사용자를 통해 요청 |

팀원이 별도 브랜치(`robot/integration` 등)로 작업하고 사용자가 병합한다.

## 패키지 두 개 — 헷갈리지 말 것

```
intelligence1_algorithm/
├── herding_controller_dual/   ★ 최종 제출본 (플랜 B). 목표점 2개 발행, 엔드게임 협공 있음
└── herding_controller/          플랜 A (구버전). 목표점 1개만 발행, 협공 없음. 비교 기준선
```

**"플랜 A / 플랜 B / 최종본" 세 개가 아니라 패키지는 둘이다.** 최종본 = 플랜 B 패키지에
협공 기능(`endgame_pincer_enabled: true`)이 켜진 상태.

## 핵심 알고리즘 — 엔드게임 협공

쥐가 덫 반경 **0.8m** 안에 들어오고 **3초간** 포획이 안 되면, 두 로봇이 덫 반대편
**±60°**로 갈라져 좌우 도주로를 동시에 막는다. `herding_planner.py::compute_endgame_pincer`.

**이것이 이 프로젝트의 존재 이유다.** 그 전까지 로봇 B의 기여가 통계적으로 0이었다
(450회 중 1건). 협공 도입 후 구조 14건 : 역행 0건, p=0.00012.

## 사용자가 정한 전제 — 반드시 지킬 것

1. **Nav2가 장애물을 피한다고 가정한다.** 시뮬레이터에서 로봇이 벽에 막혀 멈추는 건
   시뮬레이터 한계지 알고리즘 결과가 아니다. 그걸 근거로 판단하지 말 것.
2. **로봇 두 대가 모두 기여해야 한다.** 로봇 하나씩 표적을 나눠 맡는 식의 "분업"은
   협동 알고리즘이 아니므로 기각됐다. 실기에서 로봇 A는 detector 파트가 유도하므로,
   로봇 B의 기여를 증명 못 하면 우리 알고리즘을 안 쓴 것과 같다.
3. **쥐는 덫을 구별하지 못한다.** 덫을 피하는 표적 모델은 전제부터 틀렸다.
   우리가 쥐를 덫 좌표로 몰면 되고, 도달하면 물리적으로 걸려 멈춘다.
4. **맵과 차는 고정.** 항상 실제 `room_map`과 18×9cm RC카 기준으로 검증한다.

## 측정 규칙 — 이걸 어겨서 두 번 속았다

- **짝지은 시드**로 비교한다. 두 조건에 완전히 동일한 난수를 쓴다.
- **표본을 충분히** (트랩당 100회). 60회에서 +5.0%p로 보이던 개선이 150쌍에서
  −4.6%p로 뒤집힌 적이 있다.
- **다른 시드로 재현**을 확인한다.
- **대리 지표를 최적화하지 말 것.** "봉인 가능 면적"을 최적화해 장애물을 놓았다가
  성공률 92%→15.8%, "포위도"로 덫 자리를 골랐다가 panic 43%. 둘 다 최종 성공률로
  직접 고르니 해결됐다.

## 현재 검증 수치 (2026-08-09, 재-SLAM 맵)

| 항목 | 값 |
|---|---|
| 성공률 | 기본 표적 **96.0%** / 사람 조작 **98.0%** (각 n=300) |
| 로봇 B 기여 | 구조 **14** : 역행 **0**, p=0.00012 |
| 정식 검증 | 실제맵 4/4, 추상 아레나 8/8 PASS |
| 단위 테스트 | dual **177** / 플랜A **154** |

**모든 수치는 시뮬레이션이다. 실물 로봇 검증은 아직 안 했다.** 이 사실을 빠뜨리지 말 것.

## 좌표 (재-SLAM 맵 기준)

```
그리드 109 x 149, origin (-2.68, -6.08), resolution 0.05
덫 top (-2.20,-2.41) / left (-1.56, 0.74) / bottom (2.35,-3.41)
로봇 A 시작 (-2.00, 0.74)   ← 순찰 경로 첫 점
로봇 B 대기 (0.62, -5.58)   ← 순찰 경로 밖이어야 함 (경로 위면 순찰을 막아 성공률 37%)
```

옛 좌표를 쓰는 자료가 있으면 `새 = 옛 + (+0.61, +2.95) m`로 변환. 단 로봇 B 대기
지점은 변환하면 안 된다.

## 자주 쓰는 명령

```bash
cd intelligence1_algorithm/herding_controller_dual

PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest test -q   # 177 passed (30초)
python3 -m test.run_validation                                # 정식 검증 (10~20분)
python3 -m experiments.pincer_demo --trials 36 --render 2     # 협공 GIF
```

`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`은 필수다 — 빼면 `anyio` 플러그인 충돌로 수집 단계에서 죽는다.
검증은 오래 걸리므로 백그라운드로 돌리고 로그를 볼 것.

## 문서

`intelligence1_algorithm/herding_controller_dual/docs/README.md`에 목록과 상태가 있다.

- **`code_review_script.md`** — 코드 리뷰 발표 대본 (최신, 리뷰는 이걸로)
- `references.md` — 참고 논문과 우리가 바꾼 것
- `notion_summary.md` — 개발 기록 요약 (노션용)
- `run_guide.md` — 다른 컴퓨터에서 실행하는 법
- `robot_team_handoff.md` — 로봇 파트 전달사항
- `operator_protocol.md` — RC카 조작 매뉴얼 (시연 담당자 필독)
- `../herding_controller_트러블슈팅_노트.md` — 개발 경위 전체 (1,874줄)

## 다음에 할 일

1. **플랜 B 실기 검증** — 가장 큰 미검증 항목. `plan:=b`로 띄운다
2. Nav2 10Hz 목표 재전송 부하 확인 (팀원이 거리 기준 0.18m 제한 적용함)
3. 충전소가 순찰 경로 위에 있는 문제 — 실기에서 Nav2 우회 가능한지
4. 브랜치를 main에 병합 (사용자가 "아직 합치지 말자"고 함 — 지시 없이 하지 말 것)

## 작업 방식

- 커밋 메시지는 한국어. **왜 그렇게 했는지**를 적는다. 실패한 시도와 그 이유도 남긴다.
- 코드 주석도 한국어. 값이 이상해 보이는 곳일수록 근거를 남긴다(나중에 누가 "버그네"
  하고 고치면 성능이 떨어진다).
- 수치를 인용할 때는 언제 측정한 것인지 밝힌다. 맵이 바뀌면 옛 수치는 무효다.
- 확실하지 않으면 측정한다. 추측으로 결론 내지 말 것.
